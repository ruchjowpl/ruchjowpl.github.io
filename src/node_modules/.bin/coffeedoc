#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../coffeedoc/bin/coffeedoc" "$@"
  ret=$?
else 
  coffee  "$basedir/../coffeedoc/bin/coffeedoc" "$@"
  ret=$?
fi
exit $ret
