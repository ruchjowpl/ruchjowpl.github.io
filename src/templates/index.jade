extends layout

block body
  header
    h1
      img(src='images/logo.png', alt='ruchJOW.pl')
    hr
    div.download
      a(href='https://github.com/ruchjowpl') Projekt na GitHub 
        img(src='images/github.svg')   
    div.intro!= contents['intro.md'].html
  section.content
    - var sellingPoints = _.chain(contents['selling-points']._.pages).sortBy(function(item) {
    -   return item.metadata['order'];
    - }).value()
    each sellingPoint in sellingPoints
      include partials/selling-point
  footer
    p 
      ruchJOW.pl
        a(href='https://ruchjowpl') ruchJOW.pl -  przyłącz się do nas!